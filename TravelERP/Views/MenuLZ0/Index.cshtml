@model IEnumerable<TravelERP.Models.MenuLZ0>

@{
    ViewData["Title"] = "Index";
}

<h2>قائمه الاذون الرئيسيه</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">إنشاء قائمه رئيسيه</a>
</p>
<table class="table table-hover" id="MenuLZ0">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.M0_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.M0_Name)
                </td>
                <td>
                    @*<a asp-action="Edit" asp-route-id="@item.Id">تعديل</a>*@
                </td>
            </tr>
        }
    </tbody>
</table>
@section scripts
    {
    <link href="~/DataTables/css/dataTables.bootstrap4.min.css" rel="stylesheet" />
    <script src="~/DataTables/js/jquery.dataTables.js"></script>
    <script type="text/javascript" src="~/DataTables/js/dataTables.bootstrap4.js"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $('#MenuLZ0').DataTable();

        });
        $('#MenuLZ0_filter').animate({
            position: 'absolute',
            left: '30px' });
    </script>
}
